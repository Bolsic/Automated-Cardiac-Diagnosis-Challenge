# This document uses fontspec/polyglossia, which require XeLaTeX or LuaLaTeX.
# Make plain `latexmk` use XeLaTeX as well as editor integrations.
$pdf_mode = 5;

# Resolve paths relative to the source document, matching editor builds.
$do_cd = 1;
